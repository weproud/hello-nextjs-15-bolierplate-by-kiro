/**
 * Lazy Loading Components
 *
 * 지연 로딩 관련 컴포넌트들을 중앙에서 관리하고 내보냅니다.
 */

// Lazy loading components
export { LazyComponents } from '@/components/lazy/lazy-components'

// Re-export types from the central types system
export type { LazyComponentProps } from '@/types'
